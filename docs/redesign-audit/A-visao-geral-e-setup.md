# Auditoria A: Visão Geral e Setup Lab (94c4d8d x main)

Legenda de Situação: IGUAL, SÓ TEXTO/VISUAL, LÓGICA MUDOU, PERDIDO, NOVO. "não verificado" = não conferi no código.

## 1. Visão Geral

| Item | Antes | Agora | Situação | Impacto | Recomendação |
|---|---|---|---|---|---|
| Rota /api/dashboard/overview (payload) | route.ts (94c4d8d) | route.ts:582-604, 1054 | IGUAL nos números; ADITIVO | Só acrescenta `car_name` no select do gap e o campo `winnerGapBySegment`. `winnerGapByTrack`, KPIs, weekly, historical, races, streaks não mudaram. session_type=3 e caches intocados | Nada |
| KPI iRating atual / âncora | KpiCard.tsx (value, previousValue) | app/page.tsx:332-338, components/ui/KpiCard.tsx | IGUAL (dados) / SÓ VISUAL | Mesmos `kpis.*.irating.current/previousSameWeek/week`. Rótulo mudou de "S2 W7" para `prevShort + weekShort`. Perdeu o valor absoluto da season anterior ("S2 na mesma week: 2.014"): só resta a diferença | Voltar a mostrar o valor anterior na descrição (hoje diz só "Tracejado: S2") |
| Safety Rating | badge `sr-badge` com cor por classe de licença (R/D/C/B/A/P) | `Chip solid` com o texto `currentDisplay` (page.tsx:334); no celular vira cartão próprio (340) | SÓ VISUAL / PERDIDO (cor por classe) | Valor igual (`safetyRating.currentDisplay`). Sumiu a cor da classe de licença | Baixo. Restaurar cor por classe se o piloto sentir falta |
| Vitórias | kpi.wins.current/previous | page.tsx:341-346 | IGUAL | Mesma fonte (route.ts:633-643, finish_position=1 por season). O minigráfico de degraus é NOVO e vem de `data.races` no cliente (page.tsx:324): pode divergir do número se `races` for só parte dos resultados. não verificado | Conferir que o último degrau == KPI |
| Sequência (atual + recorde) | KpiCard mode streak | page.tsx:348-353 | IGUAL (dados) | `data.streaks` inalterado. Texto de zero encurtado ("Última corrida perdeu iRating"). Minigráfico de barras NOVO (últimos 12 Δ) | Nada |
| Comparação com a season anterior na mesma week | previousLabel + `W{week}` | page.tsx:310,335 (`prevShort`, `weekShort`) | IGUAL | Fórmula `current - previousSameWeek` idêntica | Nada |
| Evolução semanal (SeasonChart) | SeasonChart.tsx (eixo Y absoluto, atual x anterior, hover com iRating/delta da week, métrica iRating e Safety) | Minigráficos de 4 linhas (page.tsx:312, 337) | PERDIDO (gráfico) / dados IGUAIS | Mesmas fontes `weekly.*.current/previous`, MAS a série agora usa `iratingEnd ?? iratingFirst ?? iratingBeforeWeek` (antes só `iratingEnd`, semanas nulas eram puladas): semana sem `iratingEnd` passa a plotar outro valor. Sem hover, sem delta por semana, sem eixo, sem visão de Safety Rating por semana | PERGUNTA 1. Se quiser a análise de antes, reaproveitar SeasonChart.tsx do 94c4d8d numa aba/drawer |
| Dispersão duração x Δ iRating | RaceScatterPlot.tsx | components/overview/RaceScatter.tsx + page.tsx:259-268 | IGUAL (lógica) | Mesmos filtros (categoria, série, `durationMinutes` não nulo, só season atual). Eixo Y agora arredonda para múltiplo de 25 (antes max(25, |delta|)): só escala. Categoria padrão mudou de Formula para Sports (page.tsx:159) | Nada |
| Definição de duração | route.ts (voltas x melhor volta, servidor) | inalterada | IGUAL | Texto do subtítulo mantém "duração estimada" | Nada |
| Contextos da semana: agrupamento e dedupe Fixed/Open | page.tsx (`stripFixedSuffix`, chave `series::track`) | page.tsx:113-126, 285-307 | IGUAL | Mesma regra de match por trilha+classe, mesmo dedupe, mesma regra mínima de 2 corridas (`races >= 2`, senão "—"). Filtro por categoria (`pageFilter`) é NOVO e vale só no celular; `slice(0,3)` roda depois do filtro | Nada |
| Contextos: textos/links | "Média de Δ ... amostra mínima de duas corridas"; link "Analisar volta elegível" por card; botão "Preparar telemetria"; DataFreshness dentro da seção | Card inteiro linka para /telemetry; sem a explicação da amostra mínima | SÓ TEXTO / PERDIDO (explicação) | O critério de 2 corridas deixou de ser explicado na tela. O texto "volta elegível" (regra de elegibilidade explícita, CLAUDE.md) sumiu | Devolver a frase da amostra mínima e "volta elegível" |
| Rankings: aggregateRows | page.tsx:128-153 | page.tsx:128-153 | IGUAL (cópia idêntica) | avgDelta recalculado = total/corridas, mínimo 2, ordenação idêntica | Nada |
| Rankings: top 5 / bottom 5 | PerformanceRanking.tsx:34-36 | PerformancePanels.tsx (DeltaByContext) | IGUAL | 5 maiores ganhos + 5 maiores perdas, mesma ordem. Limiares de evidência 8/4 iguais | Nada |
| Rankings: layout | 3 painéis simultâneos (SF por pista, GT3, IMSA) | 1 painel com abas SF/GT3/IMSA (page.tsx:358-363) | SÓ VISUAL | Menos coisa visível de uma vez. Padrões mudaram: gt3Mode e imsaMode de `car` para `track` | Aceitável |
| Gap para o vencedor | WinnerGapRanking.tsx, um painel geral com `winnerGapByTrack` | PerformancePanels.tsx (GapToWinner) + `winnerGapBySegment` | LÓGICA MUDOU / NOVO | Antes: um gap geral (todas as classes). Agora: gap por segmento (SF/GT3/IMSA) conforme a aba, com o mesmo cálculo (`aggregateWinnerGapByTrack`), média ponderada por corridas igual. Segmento classifica por `car_class` do histórico (route.ts:596-604): carro sem linha no histórico some. O gap "geral" deixou de aparecer. Fallback (page.tsx:364) usa `winnerGapByTrack` em qualquer aba se o payload em cache do localStorage for antigo: mostra o gap geral sob a aba errada | PERGUNTA 2 |
| Tabela de corridas | RaceTable.tsx: categoria, Data (dd/mm/aaaa), Série, Carro, Pista, Melhor volta, Largada, Final (medalhas P1-P3), Δ iRating; paginação 10; título "Todas as corridas da temporada" | LatestRaces.tsx: Data (dd/mm), Pista, Carro, Grid, Pos., iRating; paginação 10 mantida | PERDIDO (colunas) / SÓ VISUAL | Sumiram Série (fica só em tooltip do carro), **Melhor volta** e o ano da data. O payload ainda traz `bestLap`. Não existia coluna "delta Garage61" nem "barra Delta" na versão anterior (o Δ era o de iRating): nada a restaurar aí. Título "Últimas corridas" mostra a temporada inteira paginada | Restaurar Melhor volta e Série (ao menos no desktop) |
| "Atualizar dados" | page.tsx `syncData` | lib/data-sync-action.ts | IGUAL | Sequência sync/all, sync/telemetry, sync/rating-history e mensagens idênticas, leitura texto-antes-de-JSON preservada, recarrega o dashboard no fim. NOVO: dedupe de cliques concorrentes | Nada |
| Ações do cabeçalho antigo | page.tsx: chip da season, Atualizar, favoritos iRStats/Garage61, importar calendário, DmaicReportModal, ThemeToggle | AppHeader.tsx + barra ⋯ na Visão Geral (page.tsx:380-386) | SÓ VISUAL / PERDIDO parcial | Favoritos, importar calendário e Debrief semana/season mantidos. `DmaicReportModal` (relatório) não é mais aberto na Visão Geral: virou links para /debriefs. `ThemeToggle` não é usado em lugar nenhum (claro/escuro perdido; se o redesign é só escuro, é decisão, não verificado). Rodapé "dados via Garage61" removido | PERGUNTA 3 |
| Frescura das fontes | DataFreshness.tsx (data/hora Garage61, iRStats/setups, aviso de falha recente) | AppHeader.tsx + lib/freshness.ts + lib/use-sync-status.ts | LÓGICA MUDOU (regra nova) | Mesma rota /api/sync/status. Antes: só mostrava datas e avisos. Agora: estado ok/stale/error com limiares **inventados** (Garage61 >36 h, iRStats >7 dias). O aviso "houve falha recente, último sync válido preservado" e "setups: última importação" só aparecem em tooltip; a Visão Geral perdeu o bloco visível. Rota de status ganhou o campo `driver` (aditivo) | Manter os limiares só se o piloto concordar (PERGUNTA 4). Exibir falha recente fora do tooltip |
| Cache local do dashboard | localStorage `iracing-dashboard-overview-v1` | igual | IGUAL | Chave não foi versionada apesar do campo novo `winnerGapBySegment` (vide fallback do gap) | Subir a chave para v2 |
| Regras do CLAUDE.md (session_type=3, casamento 1:1, classes) | servidor | servidor inalterado (diff de 12 linhas) | IGUAL | Nenhuma regra afetada. O cliente não refiltra session_type | Nada |

## 2. Setup Lab

| Item | Antes | Agora | Situação | Impacto | Recomendação |
|---|---|---|---|---|---|
| Seletor carro + pista | SetupLab.tsx (select `key`, inventário /api/setup/inventory) | SetupLabView.tsx:40-58, 146 | IGUAL | Mesma rota e mesma escolha padrão | Nada |
| Biblioteca local privada | SetupLab.tsx (painel aberto) | SetupLabView.tsx (dentro de `<details>` "Biblioteca local...") | SÓ VISUAL | Mesma rota /api/setup/library e mesmo `carDisplayName`. Escondida por padrão. Perdeu o aviso "Arquivos comerciais ficam no bucket privado..." | Recolocar a frase do guardrail |
| Anexar setup .sto | botão no chat (fixed/open/commercial/unknown, `setupKind`) | `uploadSetup` sempre `commercial` (SetupLabView.tsx:186) | LÓGICA MUDOU (pequena) | Upload de fixed/open via UI perde a classificação; a rota aceita `setupKind` | Confirmar que fixed/open só chegam por importação do Garage61 |
| Fixed/open | preferência por filename `/fixed/i` | preferência por `setup_kind === "fixed"` (SetupLabView.tsx:102) | LÓGICA MUDOU (pequena) | Se `setup_kind` estiver "unknown" em fixeds antigos, o padrão cai no primeiro da lista | Conferir dados; usar os dois critérios |
| Diff A/B: rota compare (regras) | compare/route.ts | compare/route.ts | IGUAL | Mesmas validações (dois setups distintos, mesmo carro/pista, mesmo carName, exige `decoded_params`), mesmo filtro `actionable`, mesmo top 5 de contribuintes | Nada |
| Contrato da rota compare | `base/comparison {id, filename}`, `changes[]`, `summary`, `analysis` | `{id, name}`, `changes[]` com `name` e `group`, `explanation` (NOVO), `summary`, `analysis` | LÓGICA MUDOU (contrato) | Consumidor único é o Setup Lab; sem outro consumidor encontrado. `summary` perdeu a nota "(N parâmetros sem regra ... omitidos)" | Ok, só registrar |
| Diff: categorias, contribuintes, contagem | painel SETUP DIFF com chips de categoria, "Maiores contribuintes" e "Como testar o pacote" | SetupComparator.tsx | PERDIDO (UI) | `analysis.topCategories`, `analysis.topContributors` e `skippedCount` continuam na resposta, mas a UI nova **não os exibe**. Sumiu também o total "N parâmetros verificados" e o aviso de omitidos sem regra. O cabeçalho conta `settable` (SetupComparator.tsx:23), antes contava todas as diferenças acionáveis | Exibir de novo categorias, contribuintes e omitidos (dados já vêm da rota) |
| Parâmetros "settable" (resultado, não ajustável) | tag "resultado, não ajustável" | tag "resultado da sessão, não é ajuste" | IGUAL | Mesmo flag `settable`. Novo: desgaste e pressão "last hot" e "gap/defl" como não ajustáveis (setup-diff.ts) | Nada |
| Omitidos sem regra | default `actionable:false, settable:true` | igual (`actionable:false`) | IGUAL | Continuam fora da lista | Nada |
| setup-diff.ts (o que mudou) | 192 linhas alteradas | mesma estrutura de `effect()` | Maioria SÓ TEXTO; **2 mudanças de lógica**: (a) `categoryOf`: `splitter` entra em aero; `brake bias` virou `brake.*bias` (mais amplo, pode capturar rótulos novos); (b) cruzamento asa/altura deixou de checar `!== "mixed"` (equivalente na prática). Textos de todas as regras foram reescritos | LÓGICA MUDOU (pequena) | Categorização de alguns parâmetros pode mudar de grupo em relação ao histórico | Aceitável; conferir que nada de "brake ... bias" fora de freio cai em brakes |
| Resumo / "como testar" | `comparativeSummary` + guia fixo de 4 passos | `comparativeSummary` (só redação) + `explainComparison` em lib/setup-explain.ts | NOVO | Camada determinística nova (sem LLM): frase única, "como você vai sentir" por fase, "quando usar", glossário, passos. É heurística de pesos (`contributionOf`) inventada no redesign, sem fonte em manual; textos decididos por pesos e limiar 0.5 | PERGUNTA 5 |
| Chat: histórico e limites | MAX_HISTORY 20, MAX_MESSAGE 4000, max_tokens 1000 | idem (chat/route.ts:62-64, 180, 186) | IGUAL | Guard-rails de custo preservados | Nada |
| Chat: guard-rail de linhas do prompt | `MAX_DECODED_ROWS` 150 | `MAX_PROMPT_ROWS` 150 aplicado depois de filtrar `is_mapped === false` e categoria display (engineer-proposal.ts:28-33) | LÓGICA MUDOU (pequena) | Teto mantido, mas o prompt agora omite parâmetros não mapeados e de display (menos linhas, menos custo). Comparações citadas limitadas a 40 linhas (engineer-prompt.ts) | Ok |
| Chat: modo meio-termo (dois setups) | `blendWithSetupId` (dois `/setup` = compara e pede meio-termo) | Setup ativo + até 2 citações (`MAX_MENTIONS`), comparadas com o ativo | LÓGICA MUDOU | Funcionalidade equivalente, redesenhada. Deixou de existir o aviso "Modo meio-termo ativo" | Ok |
| Chat: regras de segurança do engenheiro | prompt: direção sempre, nunca valor contínuo (exceção: cliques de amortecedor ±1), avisar que o .sto não é regravado, perguntar fase antes de propor | engineer-prompt.ts STYLE_RULES 4-6, CONDUZIR 1 | SÓ TEXTO com 2 pontos de atenção | (a) O aviso do .sto passou de "sempre" para "quando sugerir uma mudança". (b) O prompt diz `passos` 1 a 3, o código aceita `MAX_STEPS = 5`; a exceção dos amortecedores sumiu. Ganho de segurança: a proposta é resolvida contra as linhas reais do setup (`resolveProposal`), valor nunca inventado | Alinhar prompt (3) e código (5). Manter aviso do .sto em toda resposta com proposta |
| Chat: proposta estruturada | não existia | `<proposta>` JSON, ProposalPanel.tsx, `engineer-proposal.ts` | NOVO (pedido no mockup) | Bloco removido do texto salvo; o histórico enviado à OpenAI não contém as propostas antigas, só via `appliedProposal` | Ok |
| Persistência por season+carro+pista | engineer_conversations, upsert `driver_id,season_id,car_id,track_id` | idem (chat/route.ts) | IGUAL | Mensagens ganham campos opcionais (setupId, mentions, proposal), compatível com o histórico antigo | Nada |
| Regenerar / copiar / nova conversa | botões + DELETE | EngineerChat.tsx:126,147-148 | IGUAL | Regenerar agora reaproveita setup e citações gravados no turno do piloto. Copiar tira o bloco de proposta | Nada |
| Chips de sintoma | 4 chips ("Perde tração...", "Bate o fundo...") | 4 chips (troca de dois por "Instável na frenagem" e "Sem tração na saída") | SÓ TEXTO | Só sementes de texto | Nada |
| "Contexto automático" (lateral) | listava o que entra na análise | ProposalPanel | PERDIDO (texto informativo) | Só informativo | Baixo |

## PERGUNTAS AO PILOTO

1. Evolução semanal: o gráfico com hover e Safety Rating por semana era útil, ou os minigráficos bastam? Opções: (a) só minigráficos; (b) minigráficos + gráfico antigo num drawer. Recomendo (b), sem mudar a rota. Além disso, decidir se a semana sem `iratingEnd` pode plotar `iratingFirst`/`iratingBeforeWeek` (novo) ou volta a ser pulada/carregada do último valor (CLAUDE.md pede carry forward).
2. Gap para o vencedor: manter o painel geral de antes (todas as classes) ou só por segmento (SF/GT3/IMSA)? Recomendo por segmento e voltar a mostrar o geral como linha de resumo.
3. ThemeToggle: o redesign é só escuro? Se sim, aceito. Se não, recolocar.
4. Os limiares de "atrasado" no cabeçalho (Garage61 36 h, iRStats 7 dias) foram inventados no redesign. Mantém, ajusta ou volta a mostrar só as datas? Recomendo manter, já que o cron é diário.
5. O texto "como você vai sentir / quando usar" da comparação A/B vem de pesos heurísticos sem fonte nos manuais. Aceitável como orientação, ou prefere que a comparação use só o texto por parâmetro que já existia?

## RESTAURAR

- Tabela de corridas: colunas Melhor volta e Série (`bestLap`, `series` já no payload), ano na data (94c4d8d:components/RaceTable.tsx).
- Comparação A/B: exibir `analysis.topCategories`, `analysis.topContributors`, `skippedCount` e o total de parâmetros verificados (94c4d8d:components/SetupLab.tsx, bloco SETUP DIFF). A rota já devolve tudo.
- Visão Geral: frase de amostra mínima de 2 corridas e "volta elegível" nos contextos; valor absoluto da season anterior no KPI de iRating.
- DataFreshness: mostrar falha recente do Garage61 e data dos setups fora do tooltip (94c4d8d:components/DataFreshness.tsx).
- Subir a chave do cache `iracing-dashboard-overview-v1` para v2 (payload ganhou `winnerGapBySegment`).
- Prompt do engenheiro: aviso de .sto em toda resposta com proposta; alinhar `passos` (prompt 3 x código 5).
- Guardrail de biblioteca local: frase de arquivos comerciais privados.
- Não há o que restaurar em rotas: overview, sync/status, compare e chat mantêm a lógica e os guard-rails (fora as observações acima).

## Contagem por situação (itens das tabelas)
IGUAL 19 (incl. parciais), SÓ TEXTO/VISUAL 8, LÓGICA MUDOU 8, PERDIDO 8, NOVO 4 (minigráficos, gap por segmento, explicação determinística, proposta do engenheiro).
