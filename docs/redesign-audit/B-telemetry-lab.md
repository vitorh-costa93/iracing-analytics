# Auditoria B: Telemetry Lab (semana ativa, Race Debrief, Comparação de carros)

Antes = `94c4d8d`. Agora = `main` (13efb96). Método: leitura de `git show`/`git diff`, comparação de fórmulas e limiares, e execução dos testes existentes de `lap-analysis`, `corner-sequences`, `track-corner-layouts`, `race-debrief-laps` e `lap-corners` (42 de 42 passam; nada foi editado). Onde não deu para provar com dado real, está escrito "não verificado".

## Resumo por situação

IGUAL: 13 · SÓ TEXTO/VISUAL: 6 · LÓGICA MUDOU: 9 · PERDIDO: 11 · NOVO: 6

## Tabela

| Item | Antes (arquivo:linha) | Agora (arquivo:linha) | Situação | Impacto | Recomendação |
|---|---|---|---|---|---|
| Base do tempo perdido/ganho (integral de 1/v, grade 0,25%, escala = tempo real da volta ÷ integral, filtro v>1) | ActiveWeekTelemetry.tsx `compareTraces` (bins 0..100 passo 0,25; `scale = ownLapTime/ownIntegral`) | lib/lap-analysis.ts `compareLaps` (`GRID_STEP=0.25`, mesma `scale`, mesmo filtro `speed>1`) | IGUAL | Baixo. O `estimatedReferenceTime` e o `estimatedGap` são idênticos ao antigo para as mesmas duas voltas. | Nada. |
| Recorte do tempo perdido | 20 janelas fixas de 5% da volta; só as 6 piores com ganho > 0,008 s; só perdas; janela não faz wrap (`compareTraces`, `segments`) | Todos os trechos de curva (sequências) com janela = frenagem (150 m antes) + curva + saída (até 40 m, no máx. 30% da reta seguinte) e `parts` por curva (lap-analysis.ts:250-291; corner-sequences.ts `buildLapSections`) | LÓGICA MUDOU (pedido) | Os totais batem por construção: `straightsLostSeconds = estimatedGap - Σ sections.lostSeconds` (lap-analysis.ts:295), janelas de sequências vizinhas não se sobrepõem (`prevEnd+prevExit` = fim da janela anterior) e `Σ parts` = seção (partes contíguas). O antigo nunca fechava soma (só top-6 perdas). Ganhos agora aparecem (filtros "Onde eu perco / Meus pontos fortes"). | O resto nas retas (`straightsLostSeconds`) é calculado e **nunca exibido**: em CornerByCorner.tsx o card "saldo" mostra `-estimatedGap` (com retas) ao lado de perdidos+ganhos (sem retas) e a diferença fica sem explicação. Exibir "retas e transições: ±x s". |
| Gap da volta vs referência | `comparison.estimatedGap` + Δ velocidade média (`averageSpeedDifference`) | `comparison.estimatedGap` no cartão de contexto e no saldo; Δ velocidade média sumiu | LÓGICA IGUAL / PERDIDO (só o Δ velocidade média) | Baixo. | Opcional: voltar a mostrar Δ vel. média no popup/resumo. |
| Achados por janela (frenagem cedo/tarde, acelerador, pressão de freio, volante, marcha, rotação, RPM, linha esquerda/direita) | `compareTraces` `findings` com pesos (limiares 0,06, 0,03, 0,45, 0,5, 300 RPM, 0,3 m) | lib/lap-analysis.ts `sectionMetrics` (brakeDeltaMeters, minSpeedDelta, throttleOnDelay, exitSpeed, throttleAvg, brakePeak, steering, gearAtApex, lineOffset com lado só se 80% das amostras concordam e ≥ 1,5 m) + lib/engineer-talk.ts | LÓGICA MUDOU | Médio. Limiares e critérios são novos (ex.: linha só com 1,5 m e 80% de consistência, antes 0,3 m). RPM deixou de ser achado. Frases de conversa são pedido do piloto. | Aceitável; só registrar que RPM e "rotação" (lat. acel.) saíram. Não verificado com voltas reais se os textos escolhem o mesmo "motivo principal" que o antigo. |
| Relatório de inputs / `channelInsights` | Média de volante, RPM, acelerador e freio vs referência; nota de P2P na referência IBT (compareTraces, fim) | Não existe na tela nova | PERDIDO | Baixo/médio. | Perguntar (ver P1). |
| Traction events vs referência (destracionamento e correções vs referência) | `compareTraces` (`detectWheelspin`, `compareToReference`, `describe*VsReference`) | Removido da semana ativa | PERDIDO (o pedido do piloto citou só o debrief) | Médio se o piloto queria "microcorreções vs referência" também aqui. | Perguntar (P1). |
| Gráfico da volta representativa | 10 canais empilhados (velocidade, acelerador, freio, volante, RPM, marcha, embreagem, lat/long acel, yaw), faixa min–max por canal, linhas de curva, seta/Home/End, clique = zoom do mapa, painel de hover com 13 canais (ActiveWeekTelemetry.tsx ~1075-1145) | RepresentativeLapChart.tsx: delta acumulado + acelerador + freio; quadro de hover com acelerador, freio, velocidade e marcha; teclas ←/→ mantidas | PERDIDO (parcial) / SÓ VISUAL | Alto para quem usava volante/RPM/yaw/lat acel nessa tela. Velocidade, volante e marcha só aparecem no popup (mostradores). | Perguntar (P2). |
| Elegibilidade da volta representativa | `app/api/telemetry/active-week/route.ts` `isEligibleLap`, `withoutLikelyOvertakeLaps` (SF23, `max(0,45; MAD×3,5)`), corrida com `sessionType===3` primeiro | Rota **idêntica** (só ganhou `resolveCarContext`); cliente rejeita referência com P2P/Overtake no SF23 (`traceUsesOvertake`, ActiveWeekTelemetry.tsx:150, 215, 224) | IGUAL | Nenhum. | Nada. |
| Motivo da escolha da volta | `selectionExplanation`: "melhor volta de corrida sem P2P", "practice", tipos de sessão encontrados; legenda com data/hora e nº de amostras | Só "Volta elegível, sem P2P ativo…" fixo (ActiveWeekTelemetry.tsx:245); `selectionReason` só vai para o log de eventos | PERDIDO | Médio. O piloto não vê mais se a volta é de corrida ou de practice (a rota ainda cai para practice/fastest_clean quando não há corrida). O texto fixo diz "sem P2P" mesmo em carros sem P2P. | Restaurar a frase de `selectionExplanation` (usa dados que a rota já entrega). |
| Cobertura GPS (regra do CLAUDE.md) | Não havia checagem de GPS na volta representativa (só no servidor da comparação de carros) | `hasCompleteGps` (telemetry-trace.ts:232): ≥ 90% das amostras com GPS, início ≤ 2%, fim ≥ 98%, buracos ≤ 3% | NOVO | Positivo, mas é só um aviso de texto: não rejeita a volta nem esconde o mapa. | Aceitável. Se a regra é "rejeitar", a rota precisaria escolher outra volta. |
| Parser de CSV/IBT, despike de marcha, P2P por contagem, `interpolate` | ActiveWeekTelemetry.tsx:98-362 | lib/telemetry-trace.ts (diff = só `export` e `maxPoints`; padrão 900 igual) | IGUAL | Nenhum. O servidor usa `maxPoints: Infinity` para microcorreções. | Nada. |
| Cache do gap por contexto | Não existia | localStorage `ngt-context-gap:v1` (só número; sem chamadas novas) | NOVO | Conveniência local; respeita "Supabase-first". | Nada. |
| Upload de referência | ActiveWeekTelemetry.tsx `uploadReference` + `api/telemetry/reference` | Mesma lógica; rota só extraiu `buildReferenceBody` e devolve `status:"unavailable"` (200) em vez de 500 quando o arquivo some/limite diário | IGUAL (+ melhoria) | Nenhum. | Nada. |
| Detecção de curvas (GPS primeiro, lat. acel. reserva, nomes verificados por contagem) | `detectCorners` local; `lib/corner-detection.ts`, `lib/track-corners.ts` | `detectLapCorners` (lap-corners.ts). `corner-detection.ts` só exporta `MERGE_GAP_PCT` e `ABSOLUTE_MERGE_GAP_METERS` (sem mudar valor); `track-corners.ts` só ganhou `verifiedCornerNameCount` (nenhuma lista de nomes mexida) | IGUAL | Nenhum nas duas peças protegidas. | Nada. |
| Traçados canônicos e divisão de curvas longas vs detecção revisada | n/a | `track-corner-layouts.json` cobre 5 pistas: Interlagos 11, Zandvoort 15, Silverstone 16, Watkins Glen 13, Road Atlanta 13. Contagens = `verifiedCornerNameCount` para as 4 verificadas (11, 15, 16, 13, conferido nas listas). Road Atlanta não tem nomes. Le Mans, Algarve, Red Bull Ring e Donington ficam fora do JSON e com `splitLong:false` (lap-corners.ts:99-100) | NOVO, sem regressão comprovada | Baixo. As 4 pistas verificadas do JSON passam a ter numeração fixa igual à quantidade dos nomes (melhora sobre a detecção variável de 14–16). Pistas verificadas fora do JSON mantêm exatamente a detecção antiga. | Ponto de atenção: `lookupCornerLayout` não confere a contagem em tempo de execução; a garantia está só no gerador (`track-corner-layouts.build.test.ts` filtra `list.length === expected`). Se alguém regenerar o JSON com pistas verificadas sem esse filtro, os nomes deslocam. Um teste que compare `layout.corners.length` com `verifiedCornerNameCount` para toda entrada fecharia isso. |
| Divisão de curvas longas (> 400 m) por velocidade | n/a | `splitLongCorners` só em pista sem nomes verificados e sem layout | NOVO | Muda contagem/numeração só em pistas sem revisão manual (não verificado quais). | Nada, dentro da regra da memória. |
| Numeração igual entre telas | Cada tela com sua detecção (semana ativa 900 pts, debrief resolução cheia) | Uma função (`detectLapCorners`). Debrief passa 1500 pts, semana ativa e comparação 900 | LÓGICA MUDOU (risco) | Baixo/médio, não verificado: em pistas sem layout a contagem pode diferir entre telas por resolução do traço. | Aceitar ou usar o mesmo `maxPoints` nas três (o detector trabalha em grade, então provavelmente sem efeito). |
| Mapa: fronteira OSM real, projeção sem esticar X/Y, zoom na roda e pan, reset ao trocar de pista, alias Le Mans Historic 195→95, split de descontinuidade GPS (350 m) | TrackMap.tsx + mapa inline | components/telemetry/LapMap.tsx (`createTrackProjector`, `useMapZoomPan` com `resetKey`, `getTrackBoundary`, `splitGpsSegments` 350 m, filtro de segmentos por caixa + 120 m). `BOUNDARY_ALIASES {195:95}` intacto em track-boundaries.ts:89 | IGUAL | Nenhum. Marcadores e bolinhas mantêm tamanho na tela (1/scale). | Nada. |
| Mapa por curva do Race Debrief antigo (contorno da volta mais rápida + curvas com mapa) | RaceDebrief.tsx (`trackOutline`, mapas por curva) | Race Debrief novo não tem mapa | PERDIDO | Médio. O piloto pediu antes "localizar a variação no mapa". | Perguntar (P3). O botão "Ver contra a referência" leva à semana ativa, que tem mapa. |
| Popup de trecho | insight-popup: título/detalhe, mostradores sincronizados, barra de delta acumulado (`deltaSeries`), mapa local, anterior/próxima + setas + Esc, chips de métricas, foco no fechar | SectionPopup.tsx: título com **todas** as curvas ("Curvas 2–3–4 (nomes)"), detalhe, "quem paga e quem ganha" por curva da sequência, mostradores (`FocusedGaugePanel`), barra de delta acumulado (`lostInSectionAt`), mapa local (`LapMap variant="popup"`, ±1%), anterior/próxima, ←/→, Esc, chips | IGUAL / NOVO (sequência com partes) | O que o antigo tinha e continua: tudo. Novidade: divisão por curva dentro da sequência. Perdeu: clique no gráfico não dá zoom no mapa principal. | Nada crítico. |
| Nomes reais nas curvas do popup | `cornerLabel` = nomes verificados separados por "–", senão "Curvas a–b" | `sectionLabel`: números sempre, nomes entre parênteses (pedido do piloto em 26/09) | SÓ TEXTO | Nenhum. | Nada. |
| Seleção da corrida no debrief | Rota `/api/telemetry/debrief`: por categoria (formula/sports/GTP), última corrida com `race_results.laps ≥ 5` (`MIN_LAPS=5`), sessão Garage61 por carro+pista+±3 h, `session_type=3` | Rota `/api/telemetry/race-debrief`: lista das últimas 40 corridas com `laps ≥ 3` (`MIN_RACE_LAPS=3`, race-debrief/route.ts:35), seletor; padrão = mais recente com ≥ 3 voltas com telemetria, senão a mais recente | LÓGICA MUDOU | Médio. Mínimo caiu de 5 para 3 voltas; seleção deixou de ser "última corrida válida por categoria". A regra "só corrida" é mantida: `race_results`, `driving_sessions.session_type=3` e `laps.garage61_payload->>sessionType=3`. | Decisão dos pisos (P4). Atualizar `docs/DATA_ARCHITECTURE.md:39,87` que ainda descreve "≥5 voltas" e cache por categoria. |
| Fonte das voltas do debrief | Garage61 `/laps` ao vivo, paginado, por carro+pista (`fetchAllLaps`), com `canViewTelemetry`; **não filtrava** `sessionType` (o evento junta treino, classificação e corrida) | Tabela `laps` (Supabase-first), `sessionType=3`, telemetria via Storage com teto diário; sem chamada ao Garage61 | LÓGICA MUDOU (correção) | Positivo: remove chamada ao Garage61 por abertura e o vazamento de voltas de treino do mesmo evento (regra 1 do CLAUDE.md). Volta sem `telemetry_path` não é baixada ao vivo (a tela diz quantas). | Nada. |
| Quais voltas contam | Todas do evento sem incompleta/faltando/box; `clean=false` e fora de pista **entravam**; pool = 15 mais rápidas; `findOutlierLaps` só no lado rápido (z < −2,5, MAD×1,4826, ≥ 5 voltas); máx. 10 voltas | `classifyRaceLaps`: descarta volta 1 (largada), box, `off_track`, `clean=false`; com ≥ 5 candidatas aplica z de MAD nos **dois** lados (lenta > 2,5 = "tráfego, toque ou erro"); telemetria analisada = até 15 mais rápidas limpas | LÓGICA MUDOU | Médio. Média/ritmo mudam (voltas lentas e sujas saem). Volta rápida "provável vácuo/P2P" tem a mesma regra de z < −2,5. Não há rejeição de P2P por canal em nenhum dos dois (Garage61 não exporta esse canal no CSV). | Confirmar com o piloto que quer o filtro em duas pontas (P4). |
| Pool de voltas para setores | `sectors/route.ts`: 10 mais rápidas do evento, sem `sessionType` (bug) | `lib/sector-report.ts` (extraído): mesmo cálculo, **com** `sessionType=3` | IGUAL + correção | Positivo. Mas o pool de setores (não filtra `clean`/fora de pista/volta 1, só lado rápido) é diferente do pool do resto do debrief; o comentário "mesmo pool" não vale mais literalmente. | Alinhar ou anotar no comentário. |
| Volta ideal por setor (item e) | `buildSectorReport` (MAX 10, extra 5, MIN 5, MAD por setor z < −2,5, ideal = Σ melhores, gap = Σ setores da melhor volta − ideal) | Mesma função em `lib/sector-report.ts`, chamada por race-debrief/route.ts:205-209; exporta também os números crus | IGUAL | O "Race Debrief novo cobre volta ideal por setor com o mesmo cálculo": **sim**. A rota `/api/telemetry/sectors` foi removida sem consumidores restantes (grep no código-fonte; só sobras em `.claude/worktrees/.../.next`). | Nada. |
| Cores/limiares de consistência por setor | `consistencyLabel(sd, avg)` relativa (< 0,3% muito consistente; < 0,8%; < 1,6%) | RaceDebriefView.tsx: tom por desvio **absoluto** (≤ 0,15 s ok, ≤ 0,30 s warn, senão bad) | LÓGICA MUDOU | Médio. Em setor de 30 s o antigo dava "muito consistente" até 0,09 s; setor de 60 s, até 0,18 s. O novo trata todos os setores igual. | Voltar ao critério relativo ou justificar. `worstSector` do debrief também usa desvio absoluto, enquanto o do relatório usa razão σ/média: dois "piores setores" possíveis. |
| KPIs do debrief: resultado, Δ iRating, melhor volta, ritmo médio, incidentes | Debrief antigo mostrava só `winnerGap` e resumo por telemetria | race_results + `v_race_results_irating`; melhor volta = `fastest_lap_time` oficial (iRStats) com reserva na melhor volta limpa; incidentes = `race_results.incidents` / voltas | NOVO | Baixo. Atenção: a melhor volta oficial pode ser uma volta que o próprio debrief descartou (z < −2,5), e aí "média − melhor" incorpora essa volta. | Anotar na tela ou usar a melhor volta limpa quando divergir. |
| Cache do debrief | `race_debriefs` por categoria (3 linhas), invalida por `session_id` + `cornerDetectionVersion=5`; `winnerGap` fora do cache | `race_debriefs` com chave `race:<irstats_race_id>` (máx. 30 linhas, versão 2, `lapSetKey`), só grava em produção; cache em memória (40 entradas) | LÓGICA MUDOU | Médio: o payload em cache inclui `race.finish`, incidentes, `iratingBefore/After`, Δ iRating e `pace.reference` (vencedor). O antigo tinha deixado de propósito o dado de `race_results` fora do cache. Se o iRStats for reimportado ou a âncora de iRating (correção de 2 dias) mudar, o cache só invalida por mudança do conjunto de voltas ou da versão. Guard-rail de custo: presente (limite de 30 linhas, sem gravação fora de produção). | Compor os campos de `race_results`/iRating fora do cache (como o `winnerGap` antigo). |
| Custo por abertura do debrief | Chamada Garage61 paginada + download de até 15 CSV | `loadRaces` (40 linhas) + `matchSessions` + **todas as voltas dos ≤ 40 eventos** (paginadas por 1000) a cada GET, mesmo com payload em cache; telemetria só em cache-miss (≤ 15 arquivos + 1 referência) | LÓGICA MUDOU (risco de cota) | Médio, não medido. A lista de voltas de 40 corridas é lida em toda troca de corrida e não tem TTL próprio. Regra 7 do CLAUDE.md pede guard-rail para query pesada nova. | Cachear a lista (corridas + contagem de voltas) por 5–10 min em memória, ao estilo do cache de `dashboard/report`. |
| Ritmo: dispersão por volta ("Tempo de volta ao longo do stint") | `lapScatter`, `lapTimeSpread`, `lapTimeStddev`, `worstLap`, texto de tendência (stddev < 0,3 / 0,8) | Só melhor, média das limpas e `paceTrend` (2ª metade − 1ª, ≥ 6 voltas) que alimenta pontos fortes/melhoria; sem gráfico | PERDIDO (o mockup Debrief.dc.html também não tem; pedido explícito do piloto: não achei) | Médio. É a "dispersão do ritmo" da sua lista. | Perguntar (P5). |
| Consistência por canal (`channelStats`, score por canal, `binStats`) | Debrief antigo (throttle, brake, steering, gear, rpm) e pontos fortes/melhoria derivados dele | Substituído por `lib/self-consistency.ts`: por trecho, desvio de ponto de freio (m), retorno ao acelerador (s) e pico de volante (°), com limites 2,5/6 m, 0,12/0,25 s, 5/10° | LÓGICA MUDOU | Alto (é reescrita, não reaproveitamento). Marcha e RPM saíram; a definição de "consistente" é nova (limiares novos). O pedido do piloto foi "fundir curva a curva com consistência"; a fusão está feita, mas os cálculos antigos (bandas de freio/acelerador ±7%, apex) não foram reaproveitados. | Pedir validação dos limiares (P6). |
| Análise por curva antiga (janela ±7%: ponto de freio, velocidade mínima, retorno ao acelerador, forma, `idealLine` = melhor passagem individual naquele trecho e ganho vs média) | `computeDebrief` (CORNER_WINDOW=7, `integrateInverseSpeed`) | `analyzeSelfConsistency`: trecho por sequência; melhora = média de todas − média do terço mais rápido daquele trecho (`gainIfRepeat`), "alavanca" e em quantas voltas já acontece | LÓGICA MUDOU | Alto. O ganho deixou de ser "melhor volta individual" e virou "terço mais rápido" (mais conservador e estável, mas outro número). Curvas com nome verificado seguem os mesmos nomes. Curvas coladas agora são uma sequência só (pedido). Perdeu: as curvas de freio/acelerador da melhor passagem por curva ("copie esta") e o mapa por curva. | Perguntar (P3, P6). |
| Traction events / destracionamento e seção "Tração e volante" no debrief | `summarizeTractionEvents`, `tractionNarrative` | Removidos | REMOVIDO POR DECISÃO DO PILOTO | Nenhum. `lib/traction-events.ts` continua usado pela rota de comparação (ver linha da comparação) e pelo `local-coach`. | Ver "compute morto" abaixo. |
| Microcorreções como KPI | `tractionEvents.correctionCount` por volta | `lib/microcorrections.ts` (definição nova: inversão rápida do volante com histerese 4°, swing ≤ 0,5 s, ≥ 60 km/h, grade de 20 Hz, por minuto/por volta); KPI "Microcorreções" e coluna na comparação | NOVO (pedido) | Baixo. Definição nova, documentada, validada em 3 voltas reais segundo o comentário do arquivo (não reproduzido aqui). A rotulagem do KPI diz "referência da classe" mas o número vem da volta de **referência que o piloto enviou** para o carro/pista (race-debrief/route.ts:191-200). | Corrigir o rótulo para "sua referência" ou similar. |
| Referência x melhor volta no ritmo | n/a | `pace.reference.gap = estimatedReferenceTime − melhor volta **de telemetria**` (route:241), exibido contra a melhor volta oficial | NOVO (misto) | Baixo. Sinal coerente com o do vencedor (negativo = mais rápido que você), mas mistura duas bases de "melhor volta". | Anotar. |
| Pontos fortes / melhorias | Derivados de `channelStats` | `strengthsAndImprovements` (posições ganhas, freada repetida ≥ 70%, ritmo estável, incidentes ≥ 0,15/volta, trecho com mais `gainIfRepeat`, setor pior, microcorreções vs referência ±10/15%) | LÓGICA MUDOU (texto e critérios) | Médio. "Trecho mais redondo" = o estável com **mais voltas** (`sort by laps … pop`), não o mais estável. | Trocar o critério por menor variação. |
| Volta descartadas / outliers | `excludedOutliers` (z-score) | `discarded` com motivo (largada, box, saída de pista, incidente, lenta, rápida demais) | SÓ TEXTO/VISUAL + LÓGICA (ver "quais voltas contam") | Positivo. | Nada. |
| Comparação de carros: rota (agrupamento por tempo plausível 1,15, `MIN_PLAUSIBLE_LAP_SECONDS=20`, `off_track/pit/missing`, cobertura ≥ 85%, GPS ≥ 85% do comprimento, paginação `.range` de 1000, GTP separado de GT3, semanas, condições) | car-comparison/route.ts | Mesma rota; diff só de texto (troca de "--"), `compact=1` (remove `curves`/`gps` por trecho), microcorreções (reusa os traços já baixados) e `ownCarId`/`plausibleLapCount`/`avgLapSeconds`/`fastestLap` | IGUAL (filtros preservados) | Nenhum filtro afrouxado. Custo servidor igual ou menor; sem download novo. | Nada. |
| Comparação de carros: uso pelo cliente | 4 blocos: ranking, insight do engenheiro, mais rápido por trecho (N carros, mapa colorido), deep-dive por curva com freio/acelerador e traçado; consistência de tempo/inputs por carro; traction inline; uso de pista; condições (`conditionsNote`) | CarCompareView.tsx: ranking com melhor/média/microcorreções/delta; curva a curva **do seu carro contra 1 rival** com as duas voltas mais rápidas (`compareLaps`); mapa local; popup completo | LÓGICA MUDOU / PERDIDO | Alto. O aviso de condições diferentes (pista molhada vs seca, temperatura, borracha; `conditionsNote`) é calculado no servidor e **não é mostrado**: comparar ritmo entre carros sem esse aviso pode induzir a erro. Também sumiram: melhor por trecho entre todos os carros, consistência (tempo e inputs), uso de pista, destracionamento por carro. A comparação por curva agora usa só a volta mais rápida de cada carro (uma volta), e não a mediana de várias como o `sectors` antigo. | Restaurar o `conditionsNote` na tela (dado já vem na resposta). O resto: perguntar (P7). |
| Compute morto na comparação | n/a | A rota ainda calcula `tractionEvents`, `inputConsistency`, `trackUsage`, `narrative` para cada carro e nenhum é lido pela UI nova | PERDIDO no cliente, custo mantido no servidor | Baixo/médio (CPU do Vercel, sem custo de Storage). | Se a decisão for não mostrar, cortar do modo `compact`. |
| Texto do insight da comparação | `buildCarComparisonNarrative` | `lib/car-compare-talk.ts` (frases por trecho, rodapé de microcorreções, legenda do mapa) | SÓ TEXTO/VISUAL | Nenhum. | Nada. |
| Nome curto de carro | n/a | `lib/car-short-name.ts` | NOVO | Nenhum. | Nada. |

## Verificações pedidas, resumo direto

- (a) Totais batem. Base de tempo idêntica; a soma das seções mais o resíduo das retas é exatamente `estimatedGap`. Só falta exibir o resíduo.
- (b) Elegibilidade: regra de servidor idêntica (limpa, com telemetria, semana, sem box/off/incompleta; SF23 exclui P2P e outlier de tempo). O que sumiu foi a explicação na tela. Cobertura GPS virou aviso, não rejeição.
- (c) Debrief: mesma fonte de corrida (race_results) e mesma restrição de tipo 3, mas piso de voltas (5 → 3), pool de voltas, filtro de outliers (1 lado → 2 lados) e cálculo de consistência mudaram. Reescrito sem que se prove necessidade: consistência por canal, análise por curva (janela ±7%), dispersão do ritmo. Removido a pedido: destracionamento e "Tração e volante". Microcorreções: KPI mantido com definição nova.
- (d) Comparação: filtros de plausibilidade, GTP/GT3, paginação e semanas preservados na rota; a UI nova deixou de mostrar aviso de condições, ranking por trecho entre N carros, consistência e uso de pista.
- (e) Setores: mesmo cálculo em `lib/sector-report.ts`, com o filtro extra `sessionType=3`. Rota removida, sem consumidores.
- (f) Traçados canônicos e divisão de longas: não alteram Le Mans, Algarve, Red Bull Ring, Donington nem qualquer pista verificada fora do JSON; nas 4 verificadas do JSON a contagem bate com a lista de nomes. Único ponto frágil: falta guarda em tempo de execução/teste global.
- (g) Mapa: mantém aspect ratio, zoom/pan com reset, fronteira real, alias 195→95 e split de GPS. Testes do repositório passam.
- (h) Popup: tem tudo do antigo e mais as partes da sequência; perdeu só o zoom do mapa principal ao clicar no gráfico.

## PERGUNTAS AO PILOTO

1. Na semana ativa, o "Relatório de inputs" (média de volante, RPM, acelerador, freio) e o comparativo de destracionamento/microcorreções contra a referência sumiram. Você pediu para tirar isso só do Debrief ou também daqui? Recomendo manter microcorreções vs referência no popup/resumo da semana ativa (dado já existe em `lib/microcorrections.ts`) e deixar RPM/média de inputs de fora.
2. O gráfico da volta representativa tinha 10 canais (volante, RPM, embreagem, lat/long acel., yaw) e agora só mostra atraso, acelerador e freio (velocidade, volante e marcha só no popup). Quer os canais de volta como seção recolhível, ou o mockup é o que vale? Recomendo uma seção "Mais canais" fechada por padrão.
3. O Race Debrief novo não tem mapa nem as curvas de freio/acelerador da "melhor passagem em cada curva" (a antiga `idealLine`). Quer que volte, dentro da linha do trecho, com o mapa local da `LapMap`? Recomendo sim para os 3 trechos com mais ganho.
4. Pisos e filtros: o novo debrief aceita corridas de 3 voltas (antes 5) e descarta voltas lentas e sujas (antes só rápidas). Mantém assim? Recomendo voltar a 5 voltas para o debrief e manter o descarte em duas pontas (é mais fiel a "ritmo de corrida").
5. "Dispersão do ritmo" (gráfico por volta ao longo do stint): não está no mockup. Quer de volta? Recomendo sim, compacta, dentro do painel Ritmo, com as voltas descartadas marcadas.
6. Os limiares de "varia pouco/muito" do curva a curva do debrief (freio 2,5/6 m, acelerador 0,12/0,25 s, volante 5/10°) e o cálculo "ganho se repetir as voltas rápidas" (terço mais rápido) são novos. Valida ou prefere manter "melhor passagem individual"? Recomendo manter o terço rápido, mas mostrar também a melhor passagem.
7. Comparação de carros: quer de volta o "quem manda em cada pedaço" entre todos os carros e a consistência por carro, ou o foco é só você × 1 rival? Recomendo manter o foco atual e restaurar só o aviso de condições diferentes.

## RESTAURAR

1. Aviso de condições diferentes entre os carros (`conditionsNote`, já retornado por `car-comparison/route.ts:~1355`) em `CarCompareView.tsx`. Origem: `components/CarComparison.tsx` (94c4d8d, linhas ~353-361, 508).
2. Frase de critério da volta representativa (`selectionExplanation`: corrida sem P2P, practice, tipos de sessão) e a legenda da volta (data/hora), em `ActiveWeekTelemetry.tsx`. Origem: 94c4d8d `ActiveWeekTelemetry.tsx:52-66` e ~1016.
3. Resíduo "retas e transições" (`straightsLostSeconds`) no resumo de `CornerByCorner.tsx` e da comparação, para a soma fechar na tela.
4. Critério relativo de consistência por setor (`consistencyLabel(sd, avg)`, 0,3%/0,8%/1,6%) no lugar do limite absoluto de `RaceDebriefView.tsx`. Origem: `app/api/telemetry/sectors/route.ts` (94c4d8d).
5. Dados de `race_results`/iRating fora do payload em cache do Race Debrief (padrão do `winnerGap` antigo, `app/api/telemetry/debrief/route.ts` 94c4d8d).
6. Rótulo do KPI de microcorreções (a referência é a volta enviada pelo piloto, não "da classe").
7. Só se o piloto responder sim nas perguntas 1, 2, 3 e 5: relatório de inputs, canais extras, mapa e curvas de freio/acelerador por trecho, gráfico de dispersão do ritmo.

## Outros pontos menores

- `docs/DATA_ARCHITECTURE.md` (linhas 39 e 87) ainda descreve o Meu Debrief com "≥ 5 voltas" e cache por categoria.
- Dois races no mesmo carro/pista dentro de ±3 h podem casar com a mesma sessão Garage61 (`matchSessions`, mesma limitação da versão anterior).
- Guard-rails de custo: nenhum foi afrouxado. Sem chamadas novas ao Garage61; telemetria lida do Storage com o teto diário; cache do debrief limitado a 30 linhas e sem gravação fora de produção.
- Não verificado: qualidade das microcorreções em voltas reais além dos testes; consistência da numeração de curvas entre telas em pistas sem traçado canônico; efeito real do custo da lista de 40 corridas em produção.
