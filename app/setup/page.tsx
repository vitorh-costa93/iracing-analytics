import SetupLabView from "@/components/setup/SetupLabView";
import "../night-grid-setup.css";

export const metadata = { title: "Setup Lab • Racing Analytics" };

/** Setup Lab no Night Grid (redesign etapa 6). Referência: docs/redesign-mockup/Setup.dc.html. */
export default function SetupPage() {
  return <SetupLabView />;
}
